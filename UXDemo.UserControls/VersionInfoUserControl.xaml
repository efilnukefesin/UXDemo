<UserControl x:Class="NET.efilnukefesin.UXDemo.UserControls.VersionInfoUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ViewModels="clr-namespace:NET.efilnukefesin.UXDemo.UserControls.DesignTimeViewModels"
             xmlns:local="clr-namespace:NET.efilnukefesin.UXDemo.UserControls"
             mc:Ignorable="d" 
             d:DataContext ="{d:DesignInstance {x:Type ViewModels:VersionInfoUserControlViewModel}, IsDesignTimeCreatable=True}"
             d:DesignHeight="20" d:DesignWidth="212">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0">
            <Run Text="Version "/>
            <Run Text="{Binding Version.Version, Mode=OneWay}" FontWeight="Bold" />
            <Run Text=" // " />
            <Run Text="{Binding Version.PublishDate, StringFormat='{}{0:dd.MM.yyyy hh:mm}'}" />
        </TextBlock>
    </Grid>
</UserControl>
